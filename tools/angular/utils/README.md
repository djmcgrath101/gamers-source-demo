# angular-tools-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build angular-tools-utils` to build the library.

## Running unit tests

Run `nx test angular-tools-utils` to execute the unit tests via [Vitest](https://vitest.dev/).
