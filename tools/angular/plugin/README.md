# angular-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build angular-plugin` to build the library.

## Running unit tests

Run `nx test angular-plugin` to execute the unit tests via [Vitest](https://vitest.dev/).
